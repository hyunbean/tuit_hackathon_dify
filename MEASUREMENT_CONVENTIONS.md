# 측정 컨벤션 (Measurement Conventions)

이 문서는 hyunbean의 공개 포트폴리오 레포(ML_Final_Competition, ZooMoney, tuit_hackathon_dify, DesignThinking) 전반에서 **측정/평가 결과를 기록하고 라벨링하는 방식**을 통일하기 위한 공용 문서입니다. 프로젝트마다 도메인이 다르지만(모델 성능 지표, LLM 프롬프트 품질, UX 파이프라인 산출물 등), "언제 무엇을 측정했다고 부르는가"와 "결과를 어떤 형식으로 남기는가"는 하나의 기준을 따릅니다.

## 1. 측정 시점 라벨 (When Labels)

측정은 항상 **언제, 어떤 방식으로 얻어진 값인지**를 라벨로 명시합니다. 라벨이 없는 숫자는 "출처 불명 수치"로 간주하고 신뢰하지 않습니다.

| 라벨 | 의미 | 사용 시점 |
|------|------|-----------|
| `[실측]` (measured) | 코드를 실행해 직접 얻은 값 (테스트셋 평가, 실제 실행 로그 등) | 재현 가능한 실행 결과가 있을 때 |
| `[사후측정]` (post-hoc measured) | 개발 완료 후, 별도 스크립트/노트북으로 다시 실행해 얻은 값 | 원래 개발 당시 기록을 안 남겼지만 나중에 같은 데이터/코드로 재실행했을 때 |
| `[사후집계]` (post-hoc aggregated) | 이미 존재하는 여러 로그/기록을 취합해 계산한 값 (재실행 없음) | 원본 실행 로그는 있으나 요약 통계가 없었을 때 |
| `[사후재현]` (post-hoc reproduced) | 원본 실행 환경이 사라져 최대한 근접하게 재현한 값 (완전 동일 재현은 아님) | 원본 데이터/모델 버전이 유실되어 근사치만 가능할 때 |
| `[추정]` (estimated) | 실행 기반이 아닌, 코드 리뷰/로그 정황 등으로 추정한 값 | 실행이 불가능하거나 비용이 커서 추정만 가능할 때 |

**원칙:** 라벨은 정직성 도구입니다. 실측처럼 보이게 하려고 라벨을 생략하지 않습니다. 어떤 값이든 라벨이 없으면 이 컨벤션을 따르지 않은 것으로 간주하고 보완이 필요합니다.

## 2. 공통 지표 카테고리

레포별로 실제 쓰이는 지표는 다르지만, 아래 4개 카테고리 중 하나로 분류해 기록합니다.

1. **판별력/분류 성능 (Discrimination)** — 예: KS, Gini, AUC, PSI, lift (ML_Final_Competition)
2. **정확도/오류율 (Correctness)** — 예: 오분류율, 리젝션 재현율, 버그 재현 성공률 (ZooMoney)
3. **응답 품질/일관성 (Response Quality)** — 예: 프롬프트별 성공/실패 케이스 수, LLM 응답 일관성 체크 (tuit_hackathon_dify)
4. **프로세스/산출물 무결성 (Process Integrity)** — 예: 파이프라인 재현성, 서브에이전트 출력 체이닝 성공률, 비결정성 관측 (DesignThinking)

## 3. 결과 파일 포맷 (앞으로 사용할 공통 JSON 스키마)

새로운 측정 결과는 아래 스키마를 따르는 JSON 파일로 남기는 것을 권장합니다 (파일명 자유, 예: `eval/results/2026-xx-xx.json`):

```json
{
  "date": "YYYY-MM-DD",
  "label": "[실측] | [사후측정] | [사후집계] | [사후재현] | [추정]",
  "category": "discrimination | correctness | response_quality | process_integrity",
  "metric": "지표 이름 (예: KS, Gini, PSI, 오분류율, 성공률 등)",
  "value": 0.0,
  "unit": "% | ratio | count 등",
  "source": "실행에 사용한 스크립트/노트북/커밋 해시",
  "notes": "재현 방법, 한계, 비교 기준선 등 자유 서술"
}
```

여러 지표를 한 번에 기록할 때는 위 객체의 배열로 저장합니다. 기존에 다른 포맷(마크다운 표 등)으로 남긴 과거 기록은 소급 변환하지 않아도 되며, 새 측정부터 이 스키마를 적용합니다.

## 4. 이 문서의 적용 범위

이 문서는 4개 레포에 동일하게 배포되며, 각 레포 하단에는 "이 프로젝트에서의 적용" 절에서 그 레포의 기존 측정 파일/컨벤션과의 연결점을 명시합니다.

---

## 이 프로젝트에서의 적용

이 저장소에서 실제로 쓰인 라벨은 `[사후측정]`이다: `README.md:156`(p95/p99 지연 분포 재집계, `` `[사후측정 2026-07-28]` ``, `eval/results/*.json`의 이미 커밋된 원자료를 재집계)과 `eval/run_holdout.py:1`(`"""[사후측정 2026-07-28] 진짜 holdout 재측정 러너.`)이 그 지점이며, 이 라벨은 위 공용 컨벤션의 `[사후측정]` 행에 그대로 대응한다. `[사후집계]`/`[사후재현]`은 이 저장소에서 아직 실제로 쓰인 사례를 확인하지 못했다 — 향후 같은 성격의 값을 남길 때 위 표의 정의를 그대로 따르면 된다.
