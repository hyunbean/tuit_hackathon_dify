# 포스트모템 템플릿 (Postmortem Template)

이 템플릿은 hyunbean의 공개 포트폴리오 레포(ML_Final_Competition, ZooMoney, tuit_hackathon_dify, DesignThinking) 전반에서 **이미 겪은 실제 이슈를 사후에 구조화해서 기록**하기 위한 공용 양식입니다. 새 이슈가 생기면 이 템플릿을 복사해 `docs/postmortems/{짧은-슬러그}.md`로 저장합니다.

각 레포에 이미 산문 형태로 흩어져 있던 사고 서술(예: 데이터 리키지 발견, 재현 가능한 버그, 원인 3가지 진단, 비결정성 관측 등)을 이 형식으로 옮겨 적을 때는 **새로운 사실을 추가하지 않고, 이미 문서/코드에 존재하는 내용만** 재구성합니다.

---

## 발생 현상 (What Happened)

- 무엇이 잘못되었는지, 언제 발견되었는지를 관찰 가능한 사실 위주로 서술합니다.
- 발생 시점, 영향 범위(어떤 파이프라인/기능/결과물이 영향을 받았는지)를 포함합니다.

## 발견 경위 (How It Was Found)

- 누가/무엇이 이 문제를 처음 알아차렸는지 (코드 리뷰, 테스트 실패, 결과 이상치 관찰, 재현 시도 등).
- 발견까지 걸린 시간이나 우회 경로가 있었다면 함께 기록합니다.

## 원인 분석 (Root Cause)

- 근본 원인을 구체적으로 서술합니다 (예: 데이터 분할 시점 문제, 프롬프트 체이닝 시 상태 유실, 랜덤 시드 미고정 등).
- 원인이 여러 개라면 항목별로 나누어 기술합니다.
- 추측이 아니라 코드/로그/커밋에서 확인된 근거를 인용합니다.

## 조치 (Remediation)

- 문제를 해결하기 위해 실제로 취한 조치를 서술합니다 (코드 수정, 데이터 재분할, 검증 스크립트 추가 등).
- 조치가 부분적이었다면 그 한계도 함께 기록합니다.

## 재발 방지책 (Prevention)

- 같은 문제가 다시 생기지 않도록 추가한 장치(테스트, 체크리스트, 문서화, 프로세스 변경 등).
- 아직 적용하지 못한 재발 방지책이 있다면 "미적용" 항목으로 명시합니다.

## 교훈 (Lessons Learned)

- 이 사고에서 얻은 일반화 가능한 교훈을 1-3줄로 요약합니다.
- 이 프로젝트를 넘어 다른 프로젝트에도 적용할 수 있는 교훈이면 별도로 표시합니다.

---

*이 템플릿은 4개 레포에 동일하게 배포됩니다. 실제 사고를 기록한 완성본은 `docs/postmortems/` 하위에 레포별로 별도 저장합니다.*
